/**
 * @file 环境数据模块
 * 
 * 功能：导出axios库的版本信息。
 * 
 * 设计目的：
 * 1. 集中管理版本信息，便于维护和更新
 * 2. 提供一致的版本访问点，供库内部和外部使用
 * 3. 支持构建工具进行版本替换和优化
 * 
 * 使用场景：
 * 1. 错误信息：在错误消息中包含axios版本便于调试
 * 2. 用户代理：在请求头中标识axios版本
 * 3. 特性检测：根据版本号启用或禁用特定功能
 * 4. 调试信息：在日志中记录axios版本
 * 
 * 版本号语义：遵循语义化版本规范（SemVer）
 * - 主版本号（1）：重大变更，不向下兼容
 * - 次版本号（14）：新增功能，向下兼容
 * - 修订号（0）：Bug修复，向下兼容
 * 
 * 注意：此文件在构建过程中可能被自动更新。
 */

export const VERSION = "1.14.0";