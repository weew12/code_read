/**
 * @file 空值导出（占位符/特殊用途）
 * 
 * 功能：导出一个null值，作为模块系统中的占位符或特定场景的默认值。
 * 这可能用于：条件导入的备选值、测试中的模拟值、或框架约定的特殊标记。
 * 
 * 设计目的：确保模块导入的一致性，即使在某些情况下需要空值也能有明确的导出。
 */

// eslint-disable-next-line strict
export default null;
